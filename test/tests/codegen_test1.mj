program code1

namespace Test {

	int arr[], x, y, z;
	const int num = 5;
	{
		int add(int x, int y)
		int local;
		 {
			return x + y;
		}
		
		int sub(int x, int y) {
			return x - y;
		}
	}
}

int x;
{
	int foo() 
	int k;
	{
		k = 5;
		print(k);
		return k;
	}
	
	void main() 
	int y, z, w[], ret, arr[], i;
	char a;
	{	
		y = 5;
		//read(a);
		//read(x);
		x = 3;
		x++;
		//print(x);
		x--;
		
		//print(a);
		w = new int[5];
		arr = new int[3];
		w[0] = 2;
		w[1] = 7;
		w[2] = 4;
		w[3] = 1;
		w[4] = 2;
		
		Test::arr = new int[2];

		ret = Test::sub(4, 3);		
		
		//if (false || ret > 2 && x < 3) {
		//	x++;
		//} else {
		//	if (false || ret != 1 && x == 4) {
		//		x--;
		//	}
		//}
		
		for (x = 0; x < 3; x++) {
			for (; y < 7; y++) {
			//	if (y == 6) {
				//	break;
				//}
				continue;
				print(y);
			}
			print(x);
		}
		x = 101;
	//	[Test::arr[0],i,*arr] = w;
		Test::arr[0] = w[0];
		i = w[1];
		print(y);
		z = (Test::arr[0] + 3) * y + i + ret + w[2];
		print(x);
		print(z);
		for (i = 0; i < 2; i++) {
			print(Test::arr[i]);
		}
		for (i = 0; i < 5; i++) {
			print(w[i]);
		}
		
		print(eol);
		print(Test::sub(Test::add(3, 5), Test::add(2, 5)));
		
	}

}