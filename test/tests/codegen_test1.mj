program code1

namespace Test {

	int x, y, z;
	const int num = 5;
	{
		int add()
		int local;
		 {
			x = 3;
			y = 4;
			print(x + y);
			return x + y;
		}
		
		int sub(int x, int y) {
			return x - y;
		}
	}
}

int x;
{
	int foo() 
	int k;
	{
		k = 5;
		print(k);
		return k;
	}
	
	void main() 
	int y, z, w[], ret, arr[];
	char a;
	{	
		y = 5;
		//read(a);
		//read(x);
		x = 3;
		x++;
		//print(x);
		x--;
		
		//print(a);
		w = new int[5];
		arr = new int[3];
		w[0] = 3;
		w[1] = 7;

		ret = Test::sub(4, 3);		
		
		if (false || ret > 2 && x < 3) {
			x++;
		} else {
			if (false || ret != 1 && x == 4) {
				x--;
			}
		}
		
		for (x = 0; x < 3; x++) {
			for (; y < 7; y++) {
				if (y == 6) {
					break;
				}
				continue;
				print(y);
			}
			print(x);
		}
		[,x,y,*arr] = w;
		z = (x + 3) * y + w[0] + ret;
		
		print(z);
	}

}